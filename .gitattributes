# Normalise line endings: treat as text, checkout native, commit LF.
* text=auto eol=lf

# Explicit binaries (never normalise).
*.png binary
*.jpg binary
*.pt binary
*.pkl binary
*.joblib binary
*.cbm binary
*.parquet binary

# Keep CSV as text but do not let editors mangle it.
*.csv text eol=lf
